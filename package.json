{
  "name": "les-buttes-holiday-cottages",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test:links": "node scripts/check-links.mjs",
    "test": "npm run build && npm run test:links"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "astro": "^5.14.1",
    "sharp": "^0.34.4",
    "typescript": "^5.9.2"
  },
  "engines": { "node": ">=20.11" }
}
