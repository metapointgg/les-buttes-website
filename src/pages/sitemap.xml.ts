import type{APIRoute}from'astro';import{getCollection}from'astro:content';import site from'../data/site.json';export const GET:APIRoute=async()=>{const cottages=(await getCollection('cottages')).filter(c=>c.data.available);const paths=['/','/cottages/',...cottages.map(c=>`/cottages/${c.data.slug}/`),'/explore-guernsey/','/about/','/find-us/','/contact/','/privacy/'];return new Response(`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${paths.map(p=>`<url><loc>${new URL(p,site.url)}</loc></url>`).join('')}</urlset>`,{headers:{'Content-Type':'application/xml'}})};
