---
interface Props{eyebrow:string;title:string;intro:string} const{eyebrow,title,intro}=Astro.props;
---
<header class="page-intro"><p class="eyebrow">{eyebrow}</p><h1>{title}</h1><p class="lede">{intro}</p><slot/></header>
