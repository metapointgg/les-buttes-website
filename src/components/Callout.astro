---
import Icon from './Icon.astro'; interface Props{title:string;text:string;href:string;label:string;secondaryHref?:string;secondaryLabel?:string} const{title,text,href,label,secondaryHref,secondaryLabel}=Astro.props;
---
<section class="callout wrap"><div><p class="eyebrow eyebrow-light">Plan your stay</p><h2>{title}</h2><p>{text}</p></div><div class="callout-actions"><a class="button button-light" href={href}>{label}<Icon name="arrow" size={18}/></a>{secondaryHref&&secondaryLabel&&<a class="button button-outline" href={secondaryHref}>{secondaryLabel}</a>}</div></section>
